# Vercel / ローカル（next dev）用
# 本番・Preview では Vercel ダッシュボードの Environment Variables に同じ名前で設定する
#
# GCP サービスアカウント JSON を 1 行の Base64（改行は JSON 内の \n のまま）
# 例: base64 -i path/to/key.json | tr -d '\n'
FIREBASE_SERVICE_ACCOUNT_JSON_B64=
